# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
# `cache` (no dot) is the production runtime cache: nuxt.config.ts points storageBase there for
# non-dev builds, so running .output/server locally writes it to the repo root.
cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
sw.*
i18n/.to-delete*
server/efs*
pages-*
cypress*
.unlighthouse
outputDir

# Playwright artifacts
playwright-report
.playwright-report
test-results
.test-results
playwright/.auth
.vscode
.yarn
.github/*
!.github/workflows
!.github/instructions
!.github/agent-scripts
.github/planning*
.github/instructions/memory.md
.copilot_exports*
.github/instructions/*
!.github/instructions/default.instructions.md
# !.github/instructions/write-detailed-jira-description.md

# Agent temp files (checkpoints, drafts)
.github/agents-temp/*
!.github/agents-temp/README.md
# Ignored whole: agent scratch, checkpoints and worktrees are never part of the image or history.
# .agents/worktrees holds live git worktrees - never git clean -fdx here
.agents/
.playwright-mcp

.claude
tmp
.tmp
.github/*
.playwright-mcp/
graphify-out
backlog.jsonl
coverage/
nohup.out
